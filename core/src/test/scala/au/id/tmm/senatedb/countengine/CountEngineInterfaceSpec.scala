package au.id.tmm.senatedb.countengine

import au.id.tmm.utilities.testing.ImprovedFlatSpec

class CountEngineInterfaceSpec extends ImprovedFlatSpec {

  behaviour of "the count engine interface"

  // TODO
  it should "work" in {
    CountEngineInterface.count(0, Array(), Array())
  }
}
