task install(type: Exec, group: 'build') {
    executable 'npm'
    args = ['install']
    workingDir 'project'
}

task check(type: Exec, group: 'build') {
    dependsOn tasks.install

    executable 'npm'
    args = ['run', 'check']
    workingDir 'project'
}
